#include <iostream>

int main() {
    setlocale(LC_ALL, "RU");
    int n;
    std::cin >> n;

    if (n < 1) {
        std::cout << "Введите число >= 1\n";
        return 0;
    }

    for (int i = 1; i <= n; ++i) {
        std::cout <<  i * i ; 
    }
    std::cout << ' \n';
    return 0;
}
